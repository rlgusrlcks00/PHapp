<!-- calendar_day_background_recorded.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 기본 배경색 -->
    <solid android:color="@android:color/transparent" />

    <!-- 선택된 날짜의 배경색 -->
    <item
        android:drawable="@color/colorCalendarDayRecordedBackground" />
</shape>
